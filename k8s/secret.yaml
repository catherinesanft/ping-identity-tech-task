apiVersion: v1
kind: Secret
metadata:
  name: stockticker-secret
  namespace: default
type: Opaque
# In a real setup this key would never sit in git as plaintext - it'd
# come from a secrets manager (AWS Secrets Manager, Vault, Kubernetes
# External Secrets, take your pick) and get injected at deploy time, or
# at the very least this file would be gitignored and applied by hand.
# It's plaintext here only because the exercise brief specifically asks
# for the sample API key to be passed in via a Secret, for demonstration.
stringData:
  APIKEY: "C227WD9W3LUVKVV9"
